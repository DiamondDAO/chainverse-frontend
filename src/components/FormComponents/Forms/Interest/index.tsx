import { FormType } from "../types";
import { Interest } from "./component";
import model from "./model";
const FormInfo: FormType = {
  component: <Interest />,
  ...model,
};

export default FormInfo;
